// import * as types from '../actions/actionTypes';

export default function recordsReducer(state = [], action) {
  switch (action.type) {
  case 'CONFIRM_STUDENTS': {
    console.log(action);
  }
  }
  return state;
}
